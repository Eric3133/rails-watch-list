<h1>my list</h1>
<ul>
<% @lists.each do |list| %>
<%# <li>link_to "#{list.name}", </li> %>
<li><%= link_to "#{list.name}", list_path(list) %></li>
<% end %>
</ul>

<%= link_to "add a list", lists_new_path %>
